export const makeQueryKey = (domain: string, service: string, method: string) => [domain, service, method];

export const makeMutationKey = (domain: string, service: string, method: string) => [domain, service, method];
